spring:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    username: sa
    url: jdbc:h2:~/.h2/student
  application:
    name: student-service
eureka:
  client:
    service-url:
      defaultZone: http://discovery-service/eureka/
